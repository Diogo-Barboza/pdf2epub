# Python 3.13 is recommended.
# marker-pdf constrains Pillow<11.0.0, and Pillow only ships cp314 wheels from
# 11.3.0 onward, so on Python 3.14 pip builds Pillow from source rather than
# installing a wheel.
torch
marker-pdf==1.10.2
Pillow
regex